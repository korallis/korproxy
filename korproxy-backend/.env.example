# KorProxy Convex backend environment variables (local development)
#
# Copy this file to `korproxy-backend/.env.local` for local dev.
# For production, set these values in the Convex dashboard environment variables.
#
# NOTE: Never commit real secret values.
#

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# KorProxy main subscription prices (Stripe Price IDs)
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_YEARLY=

# Team discounted seats add-on price (Stripe Price ID)
# - Currency: GBP
# - Unit amount: 500 (pence) = £5.00
# - Recurring: monthly
#
# Stripe subscription quantity = discounted seats (excludes owner)
# Convex `teams.seatsPurchased` = total seats (includes owner) = quantity + 1
STRIPE_TEAM_PRICE_ID=

